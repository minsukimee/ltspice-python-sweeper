What the tool does: this tool sweeps one component value in the circuit and run the LTspice automatically, outputtig CSV + plot + run folders
Inputs: circuit file + sweep values
Output: CSV + plot + run folders
How to run: 
